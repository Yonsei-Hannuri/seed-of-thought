version: 0.0
os: linux
files:
  - source: /backend
    destination: /home/hannuri/backend

file_exists_behavior: RETAIN

permissions:
  - object: /
    pattern: "**"

hooks:
  ApplicationStart:
    - location: script.sh
